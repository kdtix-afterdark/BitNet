{
  "name": "bitnet-broker-mcp",
  "private": true,
  "description": "Local MCP server runtime dependencies for the BitNet broker.",
  "scripts": {
    "mcp:memory": "mcp-server-memory"
  },
  "dependencies": {
    "@modelcontextprotocol/server-memory": "2026.1.26"
  }
}
